<% content_for :title, "New cart item" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New cart item</h1>

  <%= render "form", cart_item: @cart_item %>

  <%= link_to "Back to cart items", cart_items_path, class: "ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
